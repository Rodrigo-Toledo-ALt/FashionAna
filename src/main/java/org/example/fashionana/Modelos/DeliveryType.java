package org.example.fashionana.Modelos;

/**
 * DeliveryType enum representing the PostgreSQL delivery_type enum type
 * IMPORTANT: The enum constant names must match exactly with the PostgreSQL enum values
 */
public enum DeliveryType {
    pickup,
    delivery
}
